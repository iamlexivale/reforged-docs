# Changelog

## 16 May 2024

- Added `v1/users` api to show all users
- Added `v1/user/:real-uuid` api to show user based on `real-uuid`

## 15 May 2024

- Added `v1/profiles` api to show all profiles
- Added `v1/profile/:profile-uuid` api to show profile based on `profile-uuid`
