# Changelog

## 16 May 2024

- Added `v1/players` api to show all users
- Added `v1/player/:username` api to show user based on `username`
- Added `v1/player/:username/:profile` api to show user based on `username` and `profile`
